{
  "name": "noryth",
  "version": "0.1.0",
  "private": true,
  "description": "Noryth — plataforma para gerenciamento de mesas de RPG presenciais",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently -k -n api,web -c magenta,cyan \"npm run api:dev\" \"npm run web:dev\"",
    "api:dev": "npm run start:dev --workspace apps/api",
    "api:build": "npm run build --workspace apps/api",
    "web:dev": "npm run start --workspace apps/web",
    "web:build": "npm run build --workspace apps/web",
    "build": "npm run api:build && npm run web:build"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
